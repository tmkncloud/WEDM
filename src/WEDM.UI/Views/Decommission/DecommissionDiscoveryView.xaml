<UserControl x:Class="WEDM.UI.Views.Decommission.DecommissionDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="0,8,0,0">
        <TextBlock DockPanel.Dock="Top" Text="{Binding StatusMessage}" Style="{StaticResource TypeBody}" Margin="0,0,0,12"/>
        <ListBox ItemsSource="{Binding DiscoveredItems}" />
    </DockPanel>
</UserControl>
