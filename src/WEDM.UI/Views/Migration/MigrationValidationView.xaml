<UserControl x:Class="WEDM.UI.Views.Migration.MigrationValidationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WEDM.UI.ViewModels.Migration"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:MigrationValidationViewModel, IsDesignTimeCreatable=False}">
    <StackPanel>
        <UniformGrid Columns="3" Margin="0,0,0,20">
            <Border CornerRadius="6" Padding="16" Margin="0,0,8,0"
                    Background="{StaticResource WarningSubtleBrush}">
                <StackPanel>
                    <TextBlock Text="{Binding CriticalCount, Mode=OneWay}" Style="{StaticResource TypeH1}"/>
                    <TextBlock Text="Critical risks" Style="{StaticResource TypeCaption}"/>
                </StackPanel>
            </Border>
            <Border CornerRadius="6" Padding="16" Margin="4,0,4,0"
                    Background="{StaticResource BackgroundPanelBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="{Binding HighCount, Mode=OneWay}" Style="{StaticResource TypeH1}"/>
                    <TextBlock Text="High risks" Style="{StaticResource TypeCaption}"/>
                </StackPanel>
            </Border>
            <Border CornerRadius="6" Padding="16" Margin="8,0,0,0"
                    Background="{StaticResource BackgroundPanelBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="{Binding RiskMessages.Count, Mode=OneWay}" Style="{StaticResource TypeH1}"/>
                    <TextBlock Text="Total risk items" Style="{StaticResource TypeCaption}"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <TextBlock Text="Risk register" Style="{StaticResource TypeH3}" Margin="0,0,0,12"/>
        <ItemsControl ItemsSource="{Binding RiskMessages, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="6" Padding="12,10" Margin="0,0,0,6"
                            Background="{StaticResource BackgroundPanelBrush}"
                            BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Severity}" Style="{StaticResource TypeCaption}"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Message}" Style="{StaticResource TypeBody}"/>
                                <TextBlock Text="{Binding Source}" Style="{StaticResource TypeCaption}"
                                           Foreground="{StaticResource TextMutedBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
