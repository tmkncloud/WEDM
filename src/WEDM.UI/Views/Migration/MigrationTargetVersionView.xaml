<UserControl x:Class="WEDM.UI.Views.Migration.MigrationTargetVersionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WEDM.UI.ViewModels.Migration"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:MigrationTargetVersionViewModel, IsDesignTimeCreatable=False}">
    <StackPanel>
        <TextBlock Text="Target middleware release" Style="{StaticResource TypeH2}" Margin="0,0,0,8"/>
        <TextBlock Text="{Binding UpgradePathDescription, Mode=OneWay}"
                   Style="{StaticResource TypeBody}"
                   Foreground="{StaticResource AccentPrimaryBrush}"
                   Margin="0,0,0,20"/>

        <ListBox ItemsSource="{Binding TargetOptions, Mode=OneWay}"
                 SelectedItem="{Binding SelectedTargetOption, Mode=TwoWay}"
                 BorderThickness="0"
                 Background="Transparent"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="4"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ToggleButton Style="{StaticResource BtnVersionCard}"
                                              IsChecked="{Binding IsSelected, RelativeSource={RelativeSource TemplatedParent}}"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:MigrationVersionOption}">
                    <StackPanel>
                        <TextBlock Text="{Binding ShortLabel}" Style="{StaticResource TypeH1}" TextAlignment="Center"/>
                        <TextBlock Text="{Binding DisplayName}" Style="{StaticResource TypeCaption}"
                                   TextAlignment="Center" Opacity="0.7" Margin="0,4,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Margin="0,16,0,0"
                   Text="No supported upgrade targets for the selected source.">
            <TextBlock.Style>
                <Style TargetType="TextBlock" BasedOn="{StaticResource TypeCaption}">
                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasValidTargets}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </StackPanel>
</UserControl>
